import * as React from 'react';
import Button from '@mui/material/Button';

const BasicButton = () => {
    return (
        <Button variant='contained' size='medium' color='error'>Dodaj ogłoszenie</Button>
    );
}

export default BasicButton;